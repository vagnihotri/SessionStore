# SessionStore

## Instructions

Make sure Docker is installed. 
Clone the repo, then run:

```bash
cd ~/fastapi_login_app                                                                                                                   
docker compose up --build
```
